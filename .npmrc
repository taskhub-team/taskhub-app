# Enforce stricter engine version requirements from package.json
engine-strict=true

# Save dependencies as audit-friendly lock files
save-prefix=~

# Warn about potentially harmful packages during install
unsafe-perm=true

# Enforce pnpm as the package manager
package-manager=pnpm@10.6.3